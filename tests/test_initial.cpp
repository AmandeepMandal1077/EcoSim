#include <gtest/gtest.h>

TEST(InitialSetupTest, BasicAssertions) {
    // EXPECT_EQ checks for equality.
    EXPECT_EQ(7 * 6, 42);
    // ASSERT_TRUE checks if a condition is true.
    ASSERT_TRUE(true);
}